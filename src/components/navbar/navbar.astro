---
import Container from "@components/container.astro";
import { Icon } from "astro-icon/components";
import { Astronav, MenuItems, MenuIcon } from "astro-navbar";

const menuitems = [
  {
    title: "首页",
    path: "/",
  },
  {
    title: "案例",
    path: "/case",
  },
  {
    title: "文章",
    path: "/blog",
  },
  {
    title: "关于我",
    path: "/about",
  },
  {
    title: "联系我",
    path: "/contact",
  },
];
const currentPath = Astro.url.pathname;
---

<Container>
  <header class="flex flex-col lg:flex-row justify-between items-center my-5">
    <Astronav>
      <div class="flex w-full lg:w-auto items-center justify-between">
        <a href="/" class="text-lg"
          >
          <span class="font-bold text-slate-800">孙伟伟</span><span
            class="text-slate-500">律师</span
          >
        </a>
        <div class="block lg:hidden">
          <MenuIcon class="w-4 h-4 text-gray-800" />
        </div>
      </div>
      <MenuItems class="hidden w-full lg:w-auto mt-2 lg:flex lg:mt-0">
        <ul class="flex flex-col lg:flex-row lg:gap-3">
          {
            menuitems.map((item, index) => (
              <>
                <li>
                  <a
                    href={item.path}
                    class="flex lg:px-3 py-2 items-center text-gray-600 hover:text-gray-900">
                    <span class={currentPath == item.path ? 'font-bold' : ''}> {item.title}</span>
                  </a>
                </li>
              </>
            ))
          }
        </ul>
      </MenuItems>
    </Astronav>
    <div>
      <div class="lg:flex items-center gap-4">
        <div class="flex items-center mt-2 space-x-2 text-gray-600 text-xl">
          <Icon class="text-gray-400 w-4 h-4" name="uil:phone" /> 18306481889
        </div>
      </div>
    </div>
  </header>
</Container>